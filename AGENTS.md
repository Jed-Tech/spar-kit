# Agent instructions

This workspace includes skills, tools, and repo workflows.

Before starting non-trivial work:

1. Read `tools/index.yaml` (core tools, then forge CLI for GitHub vs GitLab, then installers / repo-dependent as needed)
2. Check for a relevant skill
3. Check repo scripts / `justfile`
4. Then implement manually

Never assume no tool exists.
